# general_chat_agent

`general_chat_agent` is the default fallback package in the monorepo.

It is an LLM-backed conversational agent that handles greetings, broad chat, and fallback requests when the `frontdoor` router does not find a better specialist match.
